{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "docker compose --env-file=.env up -d",
    "start:build": "docker compose --env-file=.env up --build -d",
    "stop": "docker compose --env-file=.env down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
